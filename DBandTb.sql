CREATE DATABASE top_top;

CREATE TABLE calc (
    id SERIAL PRIMARY KEY,
    a INTEGER,
    b INTEGER,
    c INTEGER
);

CREATE TABLE minus (
    id SERIAL PRIMARY KEY,
    a INTEGER,
    b INTEGER,
    dif INTEGER
);
